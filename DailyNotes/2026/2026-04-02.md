---
created: 2026-04-02T17:25
updated: 2026-04-02T18:45
---
# 2026-04-02

## ZA AWS Deployment — Powerfleet Automation (UI + API)

Spent today setting up the ZA environment for Powerfleet Automation. Both pipelines built and deployed successfully.

### What was done today
- Added `ForceDeployZA` + `deploy_ZA` stage to `Powerfleet.Automation` API pipeline → pushed to `integration`
- Added `DeployToZA` + `deploy_ZA` stage to `Powerfleet.Automation.UI` pipeline → pushed to `integration`
- Fixed `api-urls.ts` ZA entry: was pointing to `za.mixtelematics.com` (main portal), corrected to `automation-api.za.mixtelematics.com`
- Created `ZA` environment in Azure DevOps (Pipelines → Environments) — required before pipeline can run
- Ran both pipelines with ForceDeployZA/DeployToZA = true — both deployed successfully to ZA-Config cluster
- Updated ZA UI task definition (revision 5) with real `OPENAI_API_KEY` from `Powerfleet.Automation\.env.local` — service rolling

### ⏳ One remaining item — DNS (continue Tuesday)

The `za.mixtelematics.com` Route 53 zone is **not** in the ZA AWS account. Need to switch SAML to the shared DNS account (same one used for AU DNS) and create two alias A records:

| Record | Target |
|--------|--------|
| `automation.za.mixtelematics.com` | `ZA-Config-ExternalALB-977038863.eu-west-1.elb.amazonaws.com` |
| `automation-api.za.mixtelematics.com` | `ZA-Config-ExternalALB-977038863.eu-west-1.elb.amazonaws.com` |

**Alias hosted zone ID:** `Z32O12XQLNTSW2`

Claude can do this via CLI once SAML is switched to the correct DNS account. Just say "continue ZA DNS" on Tuesday.

### Key references
- ZA AWS account: `668736068906` | Region: `eu-west-1` | Cluster: `ZA-Config`
- ADO service connection: `srvcdeploy-mix-cpt-operations-OperationsTools`
- ZA UI task def: `za-powerfleet-automation-ui:5` (OPENAI key set ✅)
- ZA API task def: `za-powerfleet-automation-api` (ASPNETCORE_ENVIRONMENT=ZA, reads appsettings.ZA.json)
- Skill updated: `C:\Projects\Skills\.agent\skills\aws-regional-deployment-skill\SKILL.md`
