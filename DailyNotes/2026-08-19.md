## Notes created today
- [[GeoTab Code Audit]] §4d-4f — corrected §4c's DB-search conclusion (a real cross-org serial/IMEI search does exist, in `Config.Api.DataAccess/MobileUnit.cs`) and reached AU's DB via AWS SSM (`aws ssm send-command` on `HSSYDCLN03`, bypassing the VPN's inability to resolve `*.production.local`). Ran the 13 known real Geotab identifiers against AU's `mobileunit.MobileUnits` — 0/13 matched, against a real 61,738-row table. §4e follow-up: found 2,635 real Geotab-shaped serials already registered under 12 other orgs (Water Corporation WA, Service Stream, FORACO Australia, etc.) — so Geotab devices ARE a working provisioned device type; the video's 13 units simply aren't onboarded anywhere yet. §4f: live-ran `test-geotab-qc.ps1` for real — confirmed a false-Pass defect worse than first documented: every downstream check (Odometer, LastCommunication, FaultCodes, Trips, IOX-presence) returns identical phantom values across 4 completely different, non-resolving serials, one second apart. Real, ready-for-Jira defect.
